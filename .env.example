# Private Integration Token from GoHighLevel
# Generate in: Settings -> Private Integrations -> Create new
GHL_PRIVATE_INTEGRATION_TOKEN=

# Optional: default Location ID used as fallback when a tool requires locationId
# and the caller did not provide one.
GHL_DEFAULT_LOCATION_ID=
